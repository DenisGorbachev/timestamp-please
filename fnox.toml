#:schema https://fnox.jdx.dev/schema.json

if_missing = "error"

[providers]
keychain = { type = "keychain", service = "timestamp-please" }
pass = { type = "password-store", prefix = "timestamp-please/" }
